@model VnuaVaccine.Areas.Admin.Models.InforScheduleModel
@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section jsFooter{

    <script>
        $('#btnSelectImage').on('click', function (e) {
            e.preventDefault();
            var finder = new CKFinder();
            finder.selectActionFunction = function (url) {
                $('#txtImage').val(url);
            };
            finder.popup();
        })
    </script>
}
@section sidebar{
    <ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">
        <hr class="sidebar-divider my-0">

        <li class="nav-item active">
            <a class="nav-link" href="/Home/Index">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>HomePage</span>
            </a>
        </li>

        <!-- Divider -->
        <hr class="sidebar-divider  my-0">
        <!-- Nav Item - Pages Collapse Menu -->
        <li class="nav-item active">
            <a class="nav-link" href="/Admin/PatientData/Index">
                <i class="fas fa-fw fa-user"></i>
                <span>List Patient</span>
            </a>
        </li>
        <hr class="sidebar-divider  my-0">
        <!-- Nav Item - Pages Collapse Menu -->
        <li class="nav-item active">
            <a class="nav-link" href="/Admin/UserData/Index">
                <i class="fas fa-fw fa-user"></i>
                <span>List User</span>
            </a>
        </li>
        <hr class="sidebar-divider  my-0">
        <!-- Nav Item - Pages Collapse Menu -->
        <li class="nav-item active">
            <a class="nav-link" href="/Admin/StaffData/Index">
                <i class="fas fa-fw fa-user"></i>
                <span>List Staff</span>
            </a>
        </li>
        <!-- Nav Item - Utilities Collapse Menu -->
        <hr class="sidebar-divider  my-0">
        <!-- Nav Item - Pages Collapse Menu -->
        <li class="nav-item active">
            <a class="nav-link" href="/Admin/VaccineSchedule/Index">
                <i class="fas fa-fw fa-comment"></i>
                <span>List Schedule</span>
            </a>
        </li>
        <!-- Divider -->
        <hr class="sidebar-divider  my-0">
        <!-- Nav Item - Pages Collapse Menu -->
        <li class="nav-item active">
            <a class="nav-link" href="/Admin/VaccineData/Index">
                <i class="fas fa-fw fa-folder"></i>
                <span>List Vaccine</span>
            </a>
        </li>

        <!-- Nav Item - Charts -->
        <!-- Divider -->
        <hr class="sidebar-divider d-none d-md-block">

        <!-- Sidebar Toggler (Sidebar) -->
        <div class="text-center d-none d-md-inline">
            <button class="rounded-circle border-0" id="sidebarToggle"></button>
        </div>

    </ul>
}
@{
    var session = (VnuaVaccine.Common.UserLogin)Session[VnuaVaccine.Common.SessionConstants.USER_SESSION];
    if (session == null || session.RoleId == 1)
    {
        Response.Redirect("~/Home/Index");
    }
}
@section bodymain{

    <div class="card-body p-0">
        <!-- Nested Row within Card Body -->
        <div class="row">
            @Html.AntiForgeryToken()
            <div class="col-lg-6">
                <div class="panel panel-default">
                    <div class="card shadow mb-4">
                        <div class="p-5">
                            <div class="card-header py-3">
                                <h2 class="m-0 font-weight-bold text-primary" style="text-align:center;">Update Vaccine</h2>
                            </div>
                            @using (Html.BeginForm("Edit", "VaccineSchedule", FormMethod.Post))
                            {
                                @Html.ValidationSummary(false, "", new { @class = "text-danger" });
                                <div class="form-group">
                                    <label for="inputEmail4">Tên bệnh nhân</label>
                                    <input type="text" class="form-control" readonly="readonly" id="inputEmail4" value="@Model.NamePatient" />
                                    @Html.HiddenFor(model => model.NamePatient)

                                </div>
                                <div class="form-group">
                                    <label for="inputEmail4">Tên vaccine</label>
                                    <input type="text" class="form-control" readonly="readonly" id="inputEmail4" value="@Model.NameVaccine" />
                                    @Html.HiddenFor(model => model.NameVaccine)

                                </div>
                                <style>
                                    .Sex:hover {
                                        cursor: pointer;
                                    }
                                </style>
                                <div class="form-group">
                                    <label for="Status">Trạng thái</label>
                                    <select class="form-control Sex" name="Status">
                                        @if (ViewBag.SexOptions != null)
                                        {
                                            foreach (var option in ViewBag.SexOptions)
                                            {
                                                <option value="@option.Value" @(option.Selected ? "selected" : "")>@option.Text</option>
                                            }
                                        }
                                        else
                                        {
                                            <option value="0"></option>
                                        }
                                    </select>
                                </div>
                                
                                <div class="form-group">
                                    QuantityStock
                                    @Html.TextBoxFor(model => model.Quantity, new { @class = "form-control form-control-user" })

                                </div>
                                if (Model.Time == null)
                                {
                                    <div class="form-group">
                                        <label for="inputEmail4">Ngày tiêm</label>
                                        @Html.TextBoxFor(model => model.Time, "{0:dd/MM/yyyy}", new { @type = "date", @class = "form-control form-control-user" })
                                    </div>
                                }
                                else
                                {
                                    <div class="form-group">
                                        <label for="inputEmail4">Ngày tiêm</label>
                                        @Html.TextBoxFor(model => model.Time, "{0:dd/MM/yyyy}", new { @type = "datetime", @class = "form-control form-control-user" })
                                    </div>
                                }
                                <div class="form-group">
                                    <label for="inputEmail4">Ngày tạo lịch tiêm</label>
                                    <input type="text" class="form-control" readonly="readonly" id="inputEmail4" value="@Model.CreateAt" />
                                    @Html.HiddenFor(model => model.CreateAt)

                                </div>
                                <div class="row">
                                    <div class="col-md-2">
                                        <a href="/Admin/VaccineSchedule/Index" class="btn btn-primary btn-user btn-block icon">
                                            Back
                                        </a>
                                    </div>
                                    <div class="col-md-8"></div>
                                    <div class="col-md-2">
                                        <a>
                                            <input style="background-color: #36b9cc" type="submit" value="Update" class="btn btn-primary btn-user btn-block" />
                                        </a>
                                    </div>
                                </div>
                            }
                            <hr>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<script>
    var loadFile = function (event) {
        var image = document.getElementById('output');
        image.src = URL.createObjectURL(event.target.files[0]);
    };
</script>


